#!/bin/bash
# Setup script for repo

# Make detect-shell.sh executable
chmod +x /workspaces/pipedrive/detect-shell.sh

# Optionally, add to PATH for current session
# export PATH="$PATH:/workspaces/pipedrive"

echo "Setup complete. You can now run 'detect-shell.sh' from /workspaces/pipedrive."
